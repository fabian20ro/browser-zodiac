{
  "name": "horror-scope",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run validate:grammar && tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "lint": "tsc --noEmit",
    "validate:grammar": "node scripts/validate-grammar.mjs",
    "test:grammar-en": "node scripts/validate-grammar.mjs en"
  },
  "devDependencies": {
    "jsdom": "^28.1.0",
    "typescript": "~5.9.3",
    "vite": "^8.0.16",
    "vitest": "^4.1.0"
  }
}
